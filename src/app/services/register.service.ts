import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class RegisterService {

  //httpclient
  constructor() { }

  register(user: any): void { // observable

    // enviar los datos del user al api rest

    // guardar los datos del usuario

    // return user;
  }
}
